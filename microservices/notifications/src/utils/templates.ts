export const getTemplate = (type: string, data: Object) : string => {

    switch (type) {
        case "test" : {
            // test 1
            break;
        }
        default : {
            break;
        }
    }

    return "";
}